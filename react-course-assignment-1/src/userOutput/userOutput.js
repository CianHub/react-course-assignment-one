import React from 'react';

const Userouput = props => {
  const styles = { color: 'blue' };

  return (
    <div style={styles}>
      <p>{props.text}</p>
      <p>{props.text}</p>
    </div>
  );
};

export default Userouput;
